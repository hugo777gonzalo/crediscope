# Frontend (Vite) — se exponen al navegador, no poner secretos aquí.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Edge Functions (backend, nunca al navegador) — se configuran con:
#   supabase secrets set --env-file .env.functions
# Ver .env.functions.example
